module: robot-skills

tests:
  - name: robot-skills-contract
    description: Verifies skill frontmatter, skill-map registration, install metadata, three-mode base skills, and thin component contracts.
    category: functional
    scopes: [pr]
    timeout_s: 120
    workdir: sdk
    command: |
      python3 robot-skills/tests/test_skill_contract.py
